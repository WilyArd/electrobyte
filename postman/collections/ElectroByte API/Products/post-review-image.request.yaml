$kind: http-request
method: POST
url: '{{product_service_url}}/api/reviews/{{review_id}}/images'
order: 14000
headers:
  - key: Authorization
    value: 'Bearer {{user_token}}'
body:
  type: formdata
  content:
    - key: image
      type: file
      src: '/absolute/path/to/review-image.png'
